version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:17.2.0-buster-browsers-legacy

    working_directory: ~/repo
    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            - v1-dependencies-
      - run: npm install
      - save_cache:
          paths:
            - node_modules
            - ~/.npm
            - ~/.cache
          key: v1-dependencies-{{ checksum "package.json" }}
      - run: CI=false npm run build
      - run:
          name: Check build directory
          command: ls /home/circleci/repo/build
      - store_artifacts:
          path: /home/circleci/repo/build
          destination: build

  deploy:
    docker:
      - image: circleci/python:3.9.7

    working_directory: ~/repo
    steps:
      - checkout
      - run: pip install awscli --user
      - run: aws configure set aws_access_key_id $AWS_ACCESS_KEY_ID
      - run: aws configure set aws_secret_access_key $AWS_SECRET_ACCESS_KEY
      - run: aws configure set default.region us-east-1
      - run: aws configure set default.output json
      - run: aws s3 cp /home/circleci/repo/build/ s3://agencybankingbuild/ --recursive

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
